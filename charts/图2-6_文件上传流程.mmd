sequenceDiagram
    participant C as 客户端
    participant S as 服务器

    C->>S: UPLOAD_REQ<br/>payload: filename(255B)+size

    S->>S: 路径校验
    S->>S: 创建文件(O_EXCL)

    S-->>C: UPLOAD_RESP (status)

    loop 分块传输 (8KB/块)
        C->>S: DATA chunk
        S->>S: 校验CRC
        S->>S: 写入文件
    end

    S-->>C: TRANSFER_RESP (status)

    Note over C,S: 任何一块校验失败或传输中断<br/>服务器删除已写入的部分文件